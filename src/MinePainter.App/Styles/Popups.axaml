<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!--
    所有原生彈出層（會自己開一個 OS 視窗的那種）一律置頂。

    浮動面板是主視窗的 owned window，永遠壓在主視窗上面；popup 也是 owned window，
    兩者同一層，面板被點過（activate）之後就會蓋住剛彈出的選單
    （使用者 2026-09-04 回報「浮窗擋住下拉式選單」）。

    注意：畫在主視窗 overlay 層的 popup（見 MainWindow.axaml 的 ShouldUseOverlayLayer）
    根本不是 OS 視窗，置頂對它沒有任何意義 —— 主選單就是因為這樣，第一次修完仍被蓋住。

    也不能用 Popup.TopmostProperty.OverrideDefaultValue —— Avalonia 已經替 Popup 自己註冊過
    這個屬性的 metadata，再覆寫會在啟動時丟「Metadata is already set for Topmost on Popup」。
  -->
  <Style Selector="Popup">
    <Setter Property="Topmost" Value="True" />
  </Style>
</Styles>
